{
  "_comment": "Task D, прогін B: копія .mcp.json БЕЗ власного сервера `catalog`. filesystem і memory лишаються підключеними — вимикається рівно одна змінна. Використовується як `claude -p ... --strict-mcp-config --mcp-config docs/mcp/mcp-run-b.json` з кореня репо.",
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem@2026.7.10",
        "./app/data"
      ]
    },
    "memory": {
      "command": "node",
      "args": ["./scripts/memory-server.mjs"]
    }
  }
}
